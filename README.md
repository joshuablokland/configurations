# Personal configuration for NVIM

Clone this repo in `~/.config/nvim/` and run `nvim`.

## Requirements
| Dependency | Version |
|------------|---------|
|lazy.nvim|---------|
| Neovim     | 0.12.0+ |
